flask
flask-cors
Pillow
numpy
# If you use OpenCV for image preprocessing:
opencv-python
# For handling Keras models and prediction:
tensorflow==2.12.0
# If you use scikit-learn for any post-processing:
scikit-learn
# (Optional) For progress bar or utilities:
tqdm
# (Optional) If you use gunicorn for running Flask in production:
gunicorn
